
module.exports = {
    publish(message: string) {
        // Empty - no need to implement.
    },

    subscribe(event: string, callback: () => {}) {
        // Empty - No need to implement.
    },
};
